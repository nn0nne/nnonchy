# yaml-language-server: $schema=https://raw.githubusercontent.com/LucasPickering/slumber/refs/tags/v4.3.1/schemas/config.json
# ^ This enables schema validation and autocomplete in your editor
# https://github.com/redhat-developer/yaml-language-server

# For configuration options, see:
# https://slumber.lucaspickering.me/api/configuration/index.html

input_bindings:
  submit: [enter]
